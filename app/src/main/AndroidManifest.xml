<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".schedulers.SchedulersActivity2" />
        <activity android:name=".schedulers.SchedulersActivity" />
        <activity android:name=".sample.LoginActivity" />
        <activity android:name=".operator.DoOperatorActivity" />
        <activity android:name=".sample.imitation.ImitationFlowActivity"></activity>
        <activity android:name=".operator.CombineLatestActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".operator.CombineLatestActivity">
        </activity>
        <activity android:name=".operator.CatchActivity">

        </activity>
    </application>

</manifest>